# Model files

Model checkpoints are stored in Google Drive and intentionally excluded from Git history.

Before running the demo, download the active CNN checkpoint to:

```text
models/dish_classifier.pt
```

`class_names.json` remains tracked because it defines the classifier output order.
